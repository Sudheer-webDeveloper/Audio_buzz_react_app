import React from 'react'
import hero  from '../../assets/hero.jpeg'

const Hero = () => {
  return (
    <section className="hero">
        <img src={hero} alt="banner_image" />
    </section>
  )
}

export default Hero
